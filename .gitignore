# ---- 凭据 / 机密（政策见 SECURITY.md）：一律放 ~/.dsh 或环境变量，仓库只写占位符 ----

# 环境变量文件（允许提交占位模板 .env.example）
.env
.env.*
!.env.example

# 私钥 / 证书 / 密钥库
*.pem
*.p12
*.pfx
*.ppk
*.jks
*.keystore
*.key
id_rsa
id_rsa.pub

# 会话 / 持久化数据（可能含用户内容）
*.sqlite
*.sqlite3
